import os
import sys

def create_file(filename, key_name, english, polish, bulgarian, langs):
    # Just creating a script to run translations via LLM is not possible since I don't have an LLM API key here.
    pass

